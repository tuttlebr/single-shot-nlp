curl -X POST "http://localhost:8080/predict" -H  "accept: application/json" -H  "Content-Type: application/json" --data-raw '{"input_string":"When thinking about automating developer workflows, the first things that come to mind for most are traditional CI/CD tasks: build, test, and deploy. However, many other common tasks can benefit from automation outside of traditional deployment pipelines.","label_options":["laryngitis", "audit", "compliance", "security", "unicorns", "automation"]}'